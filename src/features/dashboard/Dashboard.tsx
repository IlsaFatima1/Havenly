import { useEffect, useState, type ReactNode } from 'react'
import { Navigate, NavLink, Outlet, useLocation, useNavigate } from 'react-router-dom'
import { Bell, Building2, ChevronDown, CircleHelp, Compass, Home, House, LayoutDashboard, LogOut, Menu, MessageCircle, Moon, Search, Settings, Sparkles, Sun, UserRound, Users, X } from 'lucide-react'
import { useAuth } from '../../lib/auth'
import { useTheme } from '../../lib/theme'
import { Button, Skeleton, useToast } from '../../components/ui'

const baseNav = [{ label: 'Overview', path: '/dashboard', icon: Home }, { label: 'Discover', path: '/dashboard/search', icon: Compass }, { label: 'AI Studio', path: '/dashboard/ai-studio', icon: Sparkles }, { label: 'AI Matches', path: '/dashboard/ai-recommendations', icon: Sparkles }, { label: 'Messages', path: '/dashboard/messages', icon: MessageCircle }, { label: 'Notifications', path: '/dashboard/notifications', icon: Bell }, { label: 'Properties', path: '/dashboard/properties', icon: House }, { label: 'Profile', path: '/dashboard/profile', icon: UserRound }, { label: 'Team', path: '/dashboard/team', icon: Users }, { label: 'Settings', path: '/dashboard/settings', icon: Settings }]

export function ProtectedLayout() {
  const { user, loading, signOut } = useAuth(); const { isDark, setTheme } = useTheme(); const navigate = useNavigate(); const location = useLocation(); const { toast } = useToast(); const [menu, setMenu] = useState(false); const [profile, setProfile] = useState(false)
  const nav = user?.role === 'Admin' ? [{ label: 'Admin control', path: '/dashboard/admin', icon: LayoutDashboard }, ...baseNav] : baseNav
  useEffect(() => setMenu(false), [location.pathname])
  if (loading) return <div className="grid min-h-screen place-items-center bg-slate-50 dark:bg-slate-950"><div className="space-y-3 text-center"><div className="mx-auto grid size-12 animate-pulse place-items-center rounded-2xl bg-teal-600 text-white"><Building2/></div><p className="text-sm font-medium text-slate-500">Preparing your workspace…</p></div></div>
  if (!user) return <Navigate to="/login" state={{ from: location.pathname }} replace />
  const logout = () => { signOut(); toast('You have been signed out.'); navigate('/login') }
  return <div className="min-h-screen bg-[#f6f8f7] text-slate-900 dark:bg-slate-950 dark:text-slate-100"><aside className={`fixed inset-y-0 left-0 z-40 flex w-64 flex-col border-r border-slate-200 bg-white p-4 transition-transform dark:border-slate-800 dark:bg-slate-900 lg:translate-x-0 ${menu ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex h-14 items-center justify-between px-2"><div className="flex items-center gap-2 font-display text-xl font-bold"><span className="grid size-9 place-items-center rounded-xl bg-teal-600 text-white"><Building2 className="size-5"/></span>Havenly</div><button className="lg:hidden" onClick={() => setMenu(false)} aria-label="Close navigation"><X/></button></div><nav className="mt-7 space-y-1">{nav.map(({ label, path, icon: Icon }) => <NavLink key={path} to={path} end={path === '/dashboard'} className={({ isActive }) => `flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold transition ${isActive ? 'bg-teal-50 text-teal-800 dark:bg-teal-950 dark:text-teal-300' : 'text-slate-500 hover:bg-slate-50 hover:text-slate-900 dark:hover:bg-slate-800 dark:hover:text-white'}`}><Icon className="size-[18px]"/>{label}</NavLink>)}</nav><div className="mt-auto rounded-2xl bg-[#073b3a] p-4 text-white"><CircleHelp className="size-5 text-teal-300"/><p className="mt-3 text-sm font-semibold">Need a hand?</p><p className="mt-1 text-xs leading-5 text-teal-50/65">Our support team is here when you need us.</p><button onClick={() => toast('A support conversation has been opened.')} className="mt-3 text-xs font-bold text-teal-300">Contact support →</button></div></aside>{menu && <button className="fixed inset-0 z-30 bg-slate-950/40 lg:hidden" onClick={() => setMenu(false)} aria-label="Close menu"/>}<div className="lg:pl-64"><header className="sticky top-0 z-20 flex h-[72px] items-center gap-3 border-b border-slate-200 bg-white/90 px-4 backdrop-blur dark:border-slate-800 dark:bg-slate-900/90 sm:px-7"><button className="rounded-lg p-2 hover:bg-slate-100 lg:hidden" onClick={() => setMenu(true)} aria-label="Open menu"><Menu/></button><div className="relative hidden max-w-sm flex-1 sm:block"><Search className="absolute left-3 top-2.5 size-4 text-slate-400"/><input className="h-9 w-full rounded-lg bg-slate-100 pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-teal-500/30 dark:bg-slate-800" placeholder="Search workspace…"/></div><div className="ml-auto flex items-center gap-1"><button onClick={() => setTheme(isDark ? 'light' : 'dark')} className="grid size-10 place-items-center rounded-xl text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="Toggle theme">{isDark ? <Sun className="size-5"/> : <Moon className="size-5"/>}</button><button onClick={() => toast('You’re all caught up.')} className="relative grid size-10 place-items-center rounded-xl text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="Notifications"><Bell className="size-5"/><span className="absolute right-2 top-2 size-2 rounded-full bg-orange-500 ring-2 ring-white dark:ring-slate-900"/></button><div className="relative ml-2"><button onClick={() => setProfile(!profile)} className="flex items-center gap-2 rounded-xl p-1.5 hover:bg-slate-100 dark:hover:bg-slate-800"><Avatar name={user.name}/><span className="hidden text-left md:block"><span className="block text-xs font-bold">{user.name}</span><span className="block text-[11px] text-slate-400">{user.role}</span></span><ChevronDown className="size-4 text-slate-400"/></button>{profile && <div className="absolute right-0 mt-2 w-48 rounded-xl border border-slate-200 bg-white p-1.5 shadow-xl dark:border-slate-700 dark:bg-slate-900"><NavLink to="/dashboard/profile" onClick={() => setProfile(false)} className="flex items-center gap-2 rounded-lg px-3 py-2 text-sm hover:bg-slate-100 dark:hover:bg-slate-800"><UserRound className="size-4"/>Your profile</NavLink><button onClick={logout} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-sm text-rose-600 hover:bg-rose-50 dark:hover:bg-rose-950"><LogOut className="size-4"/>Sign out</button></div>}</div></div></header><main className="min-h-[calc(100vh-72px)] p-4 sm:p-7 lg:p-9"><Outlet/></main><footer className="border-t border-slate-200 px-7 py-5 text-xs text-slate-400 dark:border-slate-800"><div className="flex flex-wrap items-center justify-between gap-3"><span>© 2026 Havenly, Inc.</span><div className="flex gap-5"><button>Privacy</button><button>Security</button><button>Status</button></div></div></footer></div></div>
}

export function DashboardHome() {
  const { user } = useAuth(); const [loading, setLoading] = useState(true)
  useEffect(() => { const timer = setTimeout(() => setLoading(false), 700); return () => clearTimeout(timer) }, [])
  if (loading) return <div><Skeleton className="h-9 w-64"/><Skeleton className="mt-3 h-5 w-80"/><div className="mt-8 grid gap-4 md:grid-cols-3">{[1,2,3].map((n) => <Skeleton key={n} className="h-36"/>)}</div><Skeleton className="mt-6 h-72"/></div>
  return <div className="mx-auto max-w-6xl"><p className="text-sm font-bold uppercase tracking-wider text-teal-700 dark:text-teal-400">Workspace</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-tight sm:text-4xl">Good morning, {user?.name.split(' ')[0]}.</h1><p className="mt-2 text-slate-500">Here’s an overview of your Havenly account.</p><div className="mt-8 grid gap-4 md:grid-cols-3"><Stat label="Account status" value="Verified" detail="Identity confirmed" tone="teal"/><Stat label="Profile strength" value="80%" detail="One step remaining" tone="orange"/><Stat label="Member since" value={new Date(user?.joinedAt ?? '').toLocaleDateString('en-US', { month: 'short', year: 'numeric' })} detail="Havenly member" tone="slate"/></div><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900 sm:p-8"><div className="flex items-start justify-between gap-4"><div><h2 className="font-display text-xl font-semibold">Your workspace is ready</h2><p className="mt-1 text-sm text-slate-500">Complete these foundation steps before you get started.</p></div><span className="rounded-full bg-teal-50 px-3 py-1 text-xs font-bold text-teal-700 dark:bg-teal-950 dark:text-teal-300">2 of 3 complete</span></div><div className="mt-7 space-y-3"><Checklist done title="Create your secure account"/><Checklist done title="Verify your email address"/><Checklist title="Complete your personal profile"/></div></section></div>
}

function Stat({ label, value, detail, tone }: { label: string; value: string; detail: string; tone: 'teal'|'orange'|'slate' }) { const color = { teal: 'bg-teal-500', orange: 'bg-orange-400', slate: 'bg-slate-400' }[tone]; return <div className="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wide text-slate-400"><span className={`size-2 rounded-full ${color}`}/>{label}</div><p className="mt-4 font-display text-2xl font-semibold">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></div> }
function Checklist({ done, title }: { done?: boolean; title: string }) { return <div className="flex items-center gap-3 rounded-xl border border-slate-100 p-3 dark:border-slate-800"><span className={`grid size-6 place-items-center rounded-full text-xs font-bold ${done ? 'bg-teal-600 text-white' : 'border-2 border-slate-300 text-transparent'}`}>✓</span><span className={`text-sm font-medium ${done ? 'text-slate-400 line-through' : ''}`}>{title}</span></div> }
export function Avatar({ name }: { name: string }) { return <span className="grid size-9 place-items-center rounded-xl bg-teal-100 text-xs font-bold text-teal-800 dark:bg-teal-900 dark:text-teal-200">{name.split(' ').map((x) => x[0]).slice(0,2).join('').toUpperCase()}</span> }
export function Section({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children: ReactNode }) { return <div className="mx-auto max-w-4xl"><p className="text-sm font-bold uppercase tracking-wider text-teal-700 dark:text-teal-400">{eyebrow}</p><h1 className="mt-2 font-display text-3xl font-semibold">{title}</h1><p className="mt-2 text-slate-500">{description}</p><div className="mt-8">{children}</div></div> }
